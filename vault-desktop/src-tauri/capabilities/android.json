{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "android",
  "description": "Android-only capabilities (QR scanner)",
  "windows": ["main"],
  "platforms": ["android"],
  "permissions": [
    "barcode-scanner:default"
  ]
}
